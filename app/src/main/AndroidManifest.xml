<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.huseyinfadullahgungor.dairejsonfragmentslistview"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />
    
    
    

    <application
        android:label="@string/app_name" >
        
        <activity android:name=".Splashscreen">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        </activity>

        <activity
            android:configChanges="keyboardHidden|orientation"
            android:name=".AndroidTabAndListView"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Inbox Activity -->
        <activity android:name=".BinaAylikFatura" />

        <!-- Outbox Activity -->
        <activity android:name=".BinaDaireListesi" />

        <!-- Profile Activity -->
        <activity android:name=".ProfileActivity" />

        <!-- Sayac Activity -->
        <activity android:name=".Sayacs" />

        <activity android:name=".DaireAylikFatura" />

        <activity android:name=".GrafikBina" />

        <activity android:name=".GrafikDaire" />

        <activity android:name=".DialogActivity" />

        <activity android:name=".DigerSayaclar" />

        <activity android:name=".DigerFaturalar" />










    </application>

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>